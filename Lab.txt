Always git pull first to get the latest version
pull > change > push

origin is the online version
master is the main area where we work
add > commit > push

git add -A
git commit -am "hello"
git push origin master

git clean -nd //test
git clean -fd //delete

gitk //graph 
gitk --all

.gitignore > "stuff you want ignored"
