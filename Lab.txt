Always git pull first to get the latest version
pull > change > push

origin is the online version
master is the main area where we work
add > commit > push

git add -A
git commit -am "hello"
git push origin master

git clean -nd //test
git clean -fd //delete

gitk //graph 
gitk --all

.gitignore > "stuff you want ignored"

git log
git checkout 4A5B
commits are snap shots
you can go back to see what it looked like

git remote (-v)//origin
git branch //* is head(master) where everything sommited is put

cechoi@5013-w16:~/CMPUT301/projects/gitdemo$ git pull
Already up-to-date.
cechoi@5013-w16:~/CMPUT301/projects/gitdemo$ git branch database
cechoi@5013-w16:~/CMPUT301/projects/gitdemo$ git branch
  database
* master
cechoi@5013-w16:~/CMPUT301/projects/gitdemo$ git checkout database
Switched to branch 'database'
cechoi@5013-w16:~/CMPUT301/projects/gitdemo$ git branch
* database
  master

git merge database // make sure you're in master branch

if working on another person's repository. You can clone it but you can not push to it without credentials
